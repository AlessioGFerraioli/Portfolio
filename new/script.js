/*
let emptyElement=document.querySelector(".about .empty"),
titleElement=document.querySelector(".about .title"),
figureElements=document.querySelectorAll(".service figure");

function handleMouseMove(e) {
    emptyElement.style.flexBasis = e.clientX+"px",
    titleElement.style.flexBasis = e.clientY/2+"px",
    figureElements.forEach( function (t) {
        t.style.flexBasis=window.innerWidth-e.clientX+"px"}
    )}

//console.log(figureElements),
window.addEventListener("mousemove",handleMouseMove);

*/